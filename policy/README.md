# Org Policy (mirror)

This file is a **mirror** of the live policy at
[`DevExpGbb/.github/apm-policy.yml`](https://github.com/DevExpGbb/.github/blob/main/apm-policy.yml).

APM auto-discovers the live one via the git remote of the consumer project. This
mirror exists for two reasons:

1. **Transparency** — show the audience what governs Demo 3 without having to
   `gh browse` (helpful when the venue's network blocks GitHub).
2. **Offline reference** — read it on the plane.

If you change the live policy, update this mirror too (or the demo drifts).
