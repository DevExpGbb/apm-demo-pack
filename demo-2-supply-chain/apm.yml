name: apm-demo-pack-supply-chain
version: "1.0.0"
description: "Demo 2 -- audit + install-block fixture"

dependencies:
  apm:
    # devops-oncall is used by verify.sh: it has 'unusual characters' findings
    # AND is not denied by DevExpGbb's policy (azure-cloud-development is denied
    # for Demo 3, so Demo 2 can't use it from inside this clone).
    # On stage, the presenter audits Demo 1's directory which has azure-cloud-development.
    - github/awesome-copilot/plugins/devops-oncall
